- # XXX もっといい方法があるはず
- lt = exhibits.first.class == LightningTalk
- if exhibits.any?
  %table
    %thead
      %tr
        %th 投票対象
        - if lt
          %th 発表順
        %th タイトル
        %th 出品者
        %th 説明
        %th
          = url_title
    %tbody
      - exhibits.each do |exhibit|
        %tr
          %td
            = f.radio_button :exhibit_id, exhibit.id
          - if lt
            %td
              = exhibit.try(:announce_number)
          %td
            = exhibit.title
          %td
            = exhibit.user.nickname
          %td
            = exhibit.description
          %td
            = exhibit_image_or_slide_url_tag(exhibit)
- else
  %p まだ何もありません
