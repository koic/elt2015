- if exhibits.any?
  %table
    %thead
      %tr
        %th タイトル
        %th 出品者
        %th 説明
        %th 画像
    %tbody
      - exhibits.each do |exhibit|
        %tr
          %td
            = exhibit.title
          %td
            = exhibit.user.nickname
          %td
            = exhibit.description
          %td
            = exhibit.image_url
- else
  %p まだ何もありません
