- if votes.any?
  %table.my-votes-table
    %thead.my-votes-table__header
      %tr
        %th 種類
        %th タイトル
        %th 出品者
        %th 説明
        -# NOTE: Vote はどちらもくるので
        %th 画像 or スライド
        %th あなたがつけたコメント
        %th
    %tbody.my-votes-table__body
      - votes.each do |vote|
        %tr
          %td.my-vote__exhibit-type
            = vote.exhibit.type
          %td.my-vote__exhibit-title
            = link_to vote.exhibit.title, edit_my_vote_path(vote)
          %td.my-vote__exhibitor-nickname
            = vote.exhibit.user.nickname
          %td.my-vote__exhibit-description
            = vote.exhibit.description
          %td.my-vote__exhibit-image
            = exhibit_image_or_slide_url_tag(vote.exhibit)
          %td.my-vote__comment-for-exhibit
            = vote.comment
          %td.my-vote__delete
            = link_to 'この投票を削除', my_vote_path(vote), method: :delete, data: {confirm: '一度削除すると元に戻せません。よろしいですか？'}
- else
  %p 投票しているものは、まだ何もありません
